# 🎵 AI Melody Bot

Telegram-бот для генерации музыки через ИИ.

## Возможности

- 🎵 Генерация музыки по описанию, своему тексту или инструментал
- 🚹🚺 Выбор мужского/женского голоса
- 🎼 Выбор стиля (рок, поп, джаз и др.)
- 🔊 Voice-превью результатов
- 📥 Скачивание MP3 за кредиты
- 💰 Оплата через Telegram Stars (⭐)
- 📚 История треков
- 🛡️ Антифрод и rate limiting

## Запуск

1. Скопируйте `.env.example` → `.env` и заполните:
```bash
cp .env.example .env
```

2. Создайте базу данных PostgreSQL:
```bash
createdb ai_melody
```

3. Запустите бот:
```bash
source venv/bin/activate
python3 main.py
```

## Структура

```
├── main.py              # Точка входа
├── app/
│   ├── config.py        # Конфигурация (.env)
│   ├── database.py      # PostgreSQL (asyncpg)
│   ├── suno_api.py      # Клиент Suno API
│   ├── keyboards.py     # Inline-клавиатуры
│   ├── states.py        # FSM-состояния
│   ├── texts.py         # Тексты (RU)
│   └── handlers/
│       ├── common.py    # /start, /help, меню
│       ├── generation.py # Генерация музыки
│       └── payments.py  # Telegram Stars
├── .env.example
└── PLAN.md
```
